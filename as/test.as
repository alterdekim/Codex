package {
    import flash.text.TextField;
    import flash.display.Sprite;

    public class TextHello extends Sprite {
        var l: int = 10.5;
        if( l >= 4.0 ) {
            l += 3;
        }
    }
}